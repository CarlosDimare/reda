name: Reda Agents — Actualizar Portal

on:
  schedule:
    - cron: "0 * * * *"
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  agents:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v6
        with:
          node-version: 22

      - name: Run agent — Internacionales
        run: |
          HOY=$(date +%Y-%m-%d)
          npx --yes -p opencode-ai opencode run --dangerously-skip-permissions \
            "Sos un agente de monitoreo de conflictos GLOBALES. Hoy es $HOY.

          1. PRIMERO lee data/acciones.json y analiza las entradas existentes de seccion 'internacionales'.
          2. BUSCA en la web acciones colectivas de HOY (solo fecha $HOY) en todo el mundo, EXCLUYENDO Argentina.
             - Si una protesta existente sigue vigente hoy, ACTUALIZA sus datos (status, fuentes, updatedAt).
             - Si encontran una protesta NUEVA de hoy que no está en el archivo, AGREGALA.
             - Si una protesta existente ya NO está activa hoy, ELIMINALA del archivo.
          3. NUNCA modifiques las entradas de seccion 'protestas_ar'.
          4. Escribi el resultado a data/acciones.json y reporta: cuantas entradas nuevas agregaste, cuantas actualizaste, cuantas eliminaste.

          Cada entrada del JSON: id, seccion, pais, bandera, hora, fecha, lugar, tipoAccion, organizaciones, motivo, status, lat, lng, fuentes, createdAt, updatedAt"

      - name: Run agent — Protestas AR
        run: |
          HOY=$(date +%Y-%m-%d)
          npx --yes -p opencode-ai opencode run --dangerously-skip-permissions \
            "Sos un agente de monitoreo de protestas en ARGENTINA. Hoy es $HOY.

          1. PRIMERO lee data/acciones.json y analiza las entradas existentes de seccion 'protestas_ar'.
          2. BUSCA en la web protestas de HOY (solo fecha $HOY) en ARGENTINA.
             - Si una protesta existente sigue vigente hoy, ACTUALIZA sus datos (status, fuentes, updatedAt).
             - Si encontran una protesta NUEVA de hoy que no está en el archivo, AGREGALA.
             - Si una protesta existente ya NO está activa hoy, ELIMINALA del archivo.
          3. NUNCA modifiques las entradas de seccion 'internacionales'.
          4. Escribi el resultado a data/acciones.json y reporta: cuantas entradas nuevas agregaste, cuantas actualizaste, cuantas eliminaste.

          Cada entrada del JSON: id, seccion, pais, bandera, hora, fecha, lugar, tipoAccion, organizaciones, motivo, status, lat, lng, fuentes, createdAt, updatedAt"

      - name: Commit and push changes
        run: |
          git config user.name "Reda Bot"
          git config user.email "bot@corresponsaldigital.ar"
          git add data/acciones.json
          if git diff --cached --quiet; then
            echo "No hay cambios nuevos"
          else
            git commit -m "Actualizacion automatica del portal (agentes)"
            git pull --rebase
            git push
          fi

  deploy:
    needs: agents
    if: always()
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v6
        with:
          node-version: 22

      - name: Install pnpm
        uses: pnpm/action-setup@v6
        with:
          version: 11

      - name: Regenerate lockfile & install
        run: |
          rm -f pnpm-lock.yaml
          pnpm install --no-frozen-lockfile

      - name: Commit updated lockfile
        run: |
          git config user.name "Reda Bot"
          git config user.email "bot@corresponsaldigital.ar"
          git add pnpm-lock.yaml
          if git diff --cached --quiet; then
            echo "Lockfile sin cambios"
          else
            git commit -m "Actualizar lockfile a formato pnpm v11"
            git pull --rebase
            git push
          fi

      - name: Build frontend
        run: pnpm --filter @workspace/web-terminal run build
        env:
          BASE_PATH: /reda/

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: artifacts/web-terminal/dist

      - name: Deploy to GitHub Pages
        uses: actions/deploy-pages@v4
